# 前后端通讯协议 v1.0

## 架构说明

```
[Vue 前端] <── HTTP ──> [中间层] 文档管理
[Vue 前端] <── WebSocket ──> [中间层] 对话流式输出
[中间层] <── Python 函数调用 ──> [纯后端：Qwen + Embedding]
```

文档管理与对话是两个独立功能，使用不同通信方式。

***

## 一、文档管理（HTTP）

### 1.1 上传文件

**请求**

* Method: `POST`

* URL: `/api/upload`

* Content-Type: `multipart/form-data`

| 字段        | 类型     | 必填 | 说明                       |
| --------- | ------ | -- | ------------------------ |
| `file`    | File   | ✅  | 上传的文件，支持 txt / md / pdf / docx / csv / xlsx / xls / html / htm / pptx / ppt |
| `user_id` | string | ✅  | 用户标识                     |

**响应（成功）**

```json
{
"status": "success",
"file_id": "report.pdf",
"filename": "report.pdf",
"file_type": "pdf",
"path": "/uploads/shared/report.pdf",
"index_status": "downloaded",
"message": "文件下载完成",
"uploaded_at": "2026-06-09T16:30:00+08:00"
}
```

**响应（失败）**

```json
{
"status": "error",
"code": "UNSUPPORTED_FILE_TYPE",
"message": "不受支持的文件格式。"
}
```

***

### 1.2 查询文件索引状态

**请求**

* Method: `GET`

* URL: `/api/file/{file_id}/status`

**响应**

```json
{
"file_id": "report.pdf",
"filename": "report.pdf",
"file_type": "pdf",
"path": "/uploads/shared/report.pdf",
"index_status": "preprocessing",
"message": "正在预处理文档",
"uploaded_at": "2026-06-09T16:30:00+08:00"
}
```

`index_status` 枚举值：

| 值          | 说明       |
| ---------- | -------- |
| `pending`  | 保留兼容状态 |
| `downloaded` | 下载/落盘完成，等待预处理 |
| `preprocessing` | 正在预处理文档 |
| `indexing` | 正在构建向量索引（兼容保留） |
| `done`     | 预处理完成，可用于对话 |
| `failed`   | 处理失败 |

***

### 1.3 删除文件

**请求**

* Method: `DELETE`

* URL: `/api/file/{file_id}`

> 当前后端中该接口仅为占位实现，后续将接入实际文件删除方案。

**响应**

```json
{
"status": "todo",
"file_id": "report.pdf",
"message": "文件删除接口暂为占位实现。"
}
```

***

### 1.4 获取文件列表

**请求**

* Method: `GET`

* URL: `/api/files?user_id=xxx`

当前上传文件物理位置为共享目录 `/uploads/shared/`。

前端文档管理页面采用**文件列表轮询**方案：

- 前端通过 `GET /api/files?user_id=xxx` 周期性刷新整个文件列表
- 仅当列表中存在未完成文件时继续轮询
- 当前建议未完成状态包括：`pending`、`downloaded`、`preprocessing`、`indexing`
- 当前建议终态包括：`done`、`failed`
- 前端可根据状态变化分别提示“下载完成”和“预处理完成”

**响应**

```json
{
"status": "success",
"user_id": "xxx",
"files": [
 {
 "file_id": "report.pdf",
 "filename": "report.pdf",
 "file_type": "pdf",
 "index_status": "preprocessing",
 "message": "正在预处理文档",
 "uploaded_at": "2026-06-09T16:30:00+08:00"
 }
]
}
```

***

## 二、对话（WebSocket）

### 连接

```
ws://host/ws/chat
```

连接建立后保持长连接，所有消息均为 JSON 字符串。

***

### 2.1 基础消息结构

所有消息（双向）统一格式：

```json
{
"request_id": "前端生成的 uuid-v4，用于匹配请求与响应",
"type": "消息类型",
"payload": {}
}
```

***

### 2.2 前端 → 后端

#### 发起对话请求

```json
{
"request_id": "abc-123",
"type": "chat",
"payload": {
 "messages": [
 { "role": "system", "content": "你是一个文档分析助手" },
 { "role": "user", "content": "帮我总结这份报告的核心内容" },
 { "role": "assistant", "content": "好的，以下是核心内容..." },
 { "role": "user", "content": "重点说一下第三章" }
 ],
 "file_ids": ["uuid-1", "uuid-2"],
 "rag": {
 "enabled": true,
 "top_k": 5
 },
 "model_config": {
 "temperature": 0.7,
 "max_tokens": 2048,
 "enable_thinking": False
 }
}
}
```

字段说明：

| 字段                             | 类型    | 必填 | 说明                             |
| ------------------------------ | ----- | -- | ------------------------------ |
| `messages`                     | array | ✅  | 完整对话历史，含 system/user/assistant |
| `file_ids`                     | array | ❌  | 本次对话关联的文件，为空则不做 RAG            |
| `rag.enabled`                  | bool  | ❌  | 是否启用文档检索，默认 true               |
| `rag.top_k`                    | int   | ❌  | 检索段落数，默认 5                     |
| `model_config.temperature`     | float | ❌  | 生成温度，默认 0.7，范围 0\~2            |
| `model_config.max_tokens`      | int   | ❌  | 最大输出 token 数，默认 2048           |
| `model_config.enable_thinking` | bool  | ❌  | 是否开启思考模式，默认False               |

***

#### 取消当前请求

```json
{
"request_id": "abc-123",
"type": "cancel"
}
```

***

#### 心跳

```json
{
"request_id": "ping-001",
"type": "ping"
}
```

***

#### 查询引用详情

当前端在 assistant 回答中识别到引用标记（如 `[r12345]`），且该回答已结束（已收到对应 `stream_end`）后，可通过同一条 WebSocket 连接查询对应 chunk 的原文详情。

```json
{
"request_id": "cite-001",
"type": "citation_detail",
"payload": {
 "chunk_uid": 12345
}
}
```

字段说明：

| 字段          | 类型  | 必填 | 说明                          |
| ----------- | --- | -- | --------------------------- |
| `chunk_uid` | int | ✅  | 引用对应 chunk 的全局唯一稳定标识 |

***

### 2.3 后端 → 前端

#### 流开始

```json
{
"request_id": "abc-123",
"type": "stream_start",
"payload": {
 "created_at": 1718000000
}
}
```

#### 流式 chunk（高频，每次一小段文字）

```json
{
"request_id": "abc-123",
"type": "stream_chunk",
"payload": {
 "delta": "这是模型输出的一段文字"
}
}
```

> 当启用 RAG 检索时，assistant 的回答正文中可以包含引用标记（例如 `[r12345]`）。该标记作为普通文本随 `stream_chunk.payload.delta` 一并返回。

#### 流结束

```json
{
"request_id": "abc-123",
"type": "stream_end",
"payload": {
 "finish_reason": "stop",
 "usage": {
 "prompt_tokens": 512,
 "completion_tokens": 256,
 "total_tokens": 768
 }
}
}
```

`finish_reason` 枚举值：

| 值           | 说明                |
| ----------- | ----------------- |
| `stop`      | 正常结束              |
| `length`    | 达到 max\_tokens 截断 |
| `cancelled` | 前端主动取消            |

***

#### 错误响应

```json
{
"request_id": "abc-123",
"type": "error",
"payload": {
 "code": "MODEL_TIMEOUT",
 "message": "模型推理超时，请重试"
}
}
```

常见错误码：

| code              | 说明                 |
| ----------------- | ------------------ |
| `MODEL_TIMEOUT`   | 模型推理超时             |
| `FILE_NOT_FOUND`  | file\_id 不存在或未完成索引 |
| `INVALID_MESSAGE` | 消息格式错误             |
| `INVALID_CITATION` | 引用参数非法，例如 `chunk_uid` 缺失或格式错误 |
| `CITATION_NOT_FOUND` | 未找到对应引用内容 |
| `INTERNAL_ERROR`  | 服务内部错误             |

***

#### 心跳回应

```json
{
"request_id": "ping-001",
"type": "pong"
}
```

***

#### 引用详情响应

后端收到 `citation_detail` 请求后，返回对应 chunk 的原文详情，用于前端弹窗或侧边信息窗展示。

```json
{
"request_id": "cite-001",
"type": "citation_detail_result",
"payload": {
 "chunk_uid": 12345,
 "source": "manual.pdf",
 "doc_id": "uuid-1",
 "text": "这是当前 chunk 的原文内容"
}
}
```

字段说明：

| 字段          | 类型     | 必填 | 说明 |
| ----------- | ------ | -- | -- |
| `chunk_uid` | int    | ✅  | 当前引用对应的 chunk 全局唯一稳定标识 |
| `source`    | string | ✅  | 原始来源文件名或来源标识 |
| `doc_id`    | string | ❌  | 文档唯一标识 |
| `text`      | string | ✅  | 当前 chunk 的原文正文 |

***

### 2.4 回答中的引用标记约定

当后端启用 RAG 检索时，模型回答正文中可包含引用标记，用于标识该句所依据的检索片段。

引用标记统一格式：

```text
[r12345]
```

其中：

- `r`：reference 前缀
- `12345`：chunk 的全局唯一稳定标识（`chunk_uid`）

说明：

1. 该标记属于 assistant 文本内容的一部分，随 `stream_chunk.payload.delta` 返回。
2. 前端应在收到对应 `stream_end` 后，再将其渲染为可点击引用。
3. 前端点击引用后，通过 `citation_detail` 请求查询原文详情。
4. 若模型输出了不符合格式的内容，前端应按普通文本处理，不应报错。

***

## 三、完整对话时序

```
前端 后端
| |
|── type: chat ──────────────> |
| | (RAG 检索 + 拼 messages)
| | (调用 Qwen 流式接口)
| <─── type: stream_start ─── |
| <─── type: stream_chunk ─── | × N
| <─── type: stream_chunk ─── |
| <─── type: stream_end ───── |
| |
|   （前端此时才允许点击引用） |
| |
|── type: citation_detail ──> |
| | (根据 chunk_uid 查询原文)
| <─── type: citation_detail_result |
| |
```

***

## 四、注意事项

1. **文件先上传，再对话**：`file_ids` 中的文件必须 `index_status == done` 才能在对话中使用。
2. **`request_id`** **由前端生成**，推荐使用 `crypto.randomUUID()`，后端原样返回用于匹配。
3. **文件二进制不走 WebSocket**，统一走 HTTP multipart 上传。
4. **心跳间隔建议 30 秒**，防止连接静默断开。
5. **messages 由前端维护完整历史**，后端无状态，每次请求都带完整上下文。
6. **assistant 回答中可包含引用标记**：格式为 `[r<chunk_uid>]`，例如 `[r12345]`。
7. **引用仅在回答结束后允许点击**：前端应在收到对应 `stream_end` 后，再触发 `citation_detail` 查询。
8. **`chunk_uid` 必须稳定**：`chunk_uid` 应为全局唯一且在当前索引生命周期内稳定的标识；若重建索引导致其变化，历史消息中的引用可能失效。
