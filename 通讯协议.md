# 前后端通讯协议 v1.0

## 架构说明

```
[Vue 前端] <── HTTP ──> [中间层] 文档管理
[Vue 前端] <── WebSocket ──> [中间层] 对话流式输出
[中间层] <── Python 函数调用 ──> [纯后端：Qwen + Embedding]
```

文档管理与对话是两个独立功能，使用不同通信方式。

***

## 一、文档管理（HTTP）

### 1.1 上传文件

**请求**

* Method: `POST`

* URL: `/api/upload`

* Content-Type: `multipart/form-data`

| 字段        | 类型     | 必填 | 说明                       |
| --------- | ------ | -- | ------------------------ |
| `file`    | File   | ✅  | 上传的文件，支持 pdf / png / jpg |
| `user_id` | string | ✅  | 用户标识                     |

**响应（成功）**

```json
{
"status": "success",
"file_id": "uuid-v4",
"filename": "report.pdf",
"file_type": "pdf",
"path": "/uploads/user_123/uuid/report.pdf",
"index_status": "pending"
}
```

**响应（失败）**

```json
{
"status": "error",
"code": "UNSUPPORTED_FILE_TYPE",
"message": "仅支持 pdf / png / jpg 格式"
}
```

***

### 1.2 查询文件索引状态

**请求**

* Method: `GET`

* URL: `/api/file/{file_id}/status`

**响应**

```json
{
"file_id": "uuid-v4",
"index_status": "done",
"message": "索引构建完成，可用于对话"
}
```

`index_status` 枚举值：

| 值          | 说明       |
| ---------- | -------- |
| `pending`  | 等待处理     |
| `indexing` | 正在构建向量索引 |
| `done`     | 索引完成，可用  |
| `failed`   | 处理失败     |

***

### 1.3 删除文件

**请求**

* Method: `DELETE`

* URL: `/api/file/{file_id}`

**响应**

```json
{
"status": "success",
"file_id": "uuid-v4"
}
```

***

### 1.4 获取文件列表

**请求**

* Method: `GET`

* URL: `/api/files?user_id=xxx`

**响应**

```json
{
"files": [
 {
 "file_id": "uuid-v4",
 "filename": "report.pdf",
 "file_type": "pdf",
 "index_status": "done",
 "uploaded_at": "2025-01-01T12:00:00Z"
 }
]
}
```

***

## 二、对话（WebSocket）

### 连接

```
ws://host/ws/chat
```

连接建立后保持长连接，所有消息均为 JSON 字符串。

***

### 2.1 基础消息结构

所有消息（双向）统一格式：

```json
{
"request_id": "前端生成的 uuid-v4，用于匹配请求与响应",
"type": "消息类型",
"payload": {}
}
```

***

### 2.2 前端 → 后端

#### 发起对话请求

```json
{
"request_id": "abc-123",
"type": "chat",
"payload": {
 "messages": [
 { "role": "system", "content": "你是一个文档分析助手" },
 { "role": "user", "content": "帮我总结这份报告的核心内容" },
 { "role": "assistant", "content": "好的，以下是核心内容..." },
 { "role": "user", "content": "重点说一下第三章" }
 ],
 "file_ids": ["uuid-1", "uuid-2"],
 "rag": {
 "enabled": true,
 "top_k": 5
 },
 "model_config": {
 "temperature": 0.7,
 "max_tokens": 2048
 },
 "enable_thinking": False
}
}
```

字段说明：

| 字段                         | 类型    | 必填 | 说明                             |
| -------------------------- | ----- | -- | ------------------------------ |
| `messages`                 | array | ✅  | 完整对话历史，含 system/user/assistant |
| `file_ids`                 | array | ❌  | 本次对话关联的文件，为空则不做 RAG            |
| `rag.enabled`              | bool  | ❌  | 是否启用文档检索，默认 true               |
| `rag.top_k`                | int   | ❌  | 检索段落数，默认 5                     |
| `model_config.temperature` | float | ❌  | 生成温度，默认 0.7，范围 0\~2            |
| `model_config.max_tokens`  | int   | ❌  | 最大输出 token 数，默认 2048           |
| `enable_thinking`          | bool  | ❌  | 是否开启思考模式，默认False               |

***

#### 取消当前请求

```json
{
"request_id": "abc-123",
"type": "cancel"
}
```

***

#### 心跳

```json
{
"request_id": "ping-001",
"type": "ping"
}
```

***

### 2.3 后端 → 前端

#### 流开始

```json
{
"request_id": "abc-123",
"type": "stream_start",
"payload": {
 "created_at": 1718000000
}
}
```

#### 流式 chunk（高频，每次一小段文字）

```json
{
"request_id": "abc-123",
"type": "stream_chunk",
"payload": {
 "delta": "这是模型输出的一段文字"
}
}
```

#### 流结束

```json
{
"request_id": "abc-123",
"type": "stream_end",
"payload": {
 "finish_reason": "stop",
 "usage": {
 "prompt_tokens": 512,
 "completion_tokens": 256,
 "total_tokens": 768
 }
}
}
```

`finish_reason` 枚举值：

| 值           | 说明                |
| ----------- | ----------------- |
| `stop`      | 正常结束              |
| `length`    | 达到 max\_tokens 截断 |
| `cancelled` | 前端主动取消            |

***

#### 错误响应

```json
{
"request_id": "abc-123",
"type": "error",
"payload": {
 "code": "MODEL_TIMEOUT",
 "message": "模型推理超时，请重试"
}
}
```

常见错误码：

| code              | 说明                 |
| ----------------- | ------------------ |
| `MODEL_TIMEOUT`   | 模型推理超时             |
| `FILE_NOT_FOUND`  | file\_id 不存在或未完成索引 |
| `INVALID_MESSAGE` | 消息格式错误             |
| `INTERNAL_ERROR`  | 服务内部错误             |

***

#### 心跳回应

```json
{
"request_id": "ping-001",
"type": "pong"
}
```

***

## 三、完整对话时序

```
前端 后端
| |
|── type: chat ──────────────> |
| | (RAG 检索 + 拼 messages)
| | (调用 Qwen 流式接口)
| <─── type: stream_start ─── |
| <─── type: stream_chunk ─── | × N
| <─── type: stream_chunk ─── |
| <─── type: stream_end ───── |
| |
```

***

## 四、注意事项

1. **文件先上传，再对话**：`file_ids` 中的文件必须 `index_status == done` 才能在对话中使用。
2. **`request_id`** **由前端生成**，推荐使用 `crypto.randomUUID()`，后端原样返回用于匹配。
3. **文件二进制不走 WebSocket**，统一走 HTTP multipart 上传。
4. **心跳间隔建议 30 秒**，防止连接静默断开。
5. **messages 由前端维护完整历史**，后端无状态，每次请求都带完整上下文。
